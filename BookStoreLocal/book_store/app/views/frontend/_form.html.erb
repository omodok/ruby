	<h1>Omodok書籍管理プログラム</h1>
<span style="font-size: small"><b>
全項目を埋めて表紙画像を添付してから送信ボタンで追加して下さい。</b></span>

<%
if request.path_info == frontend_index_path
  urlPath = {:action => :create}
elsif  request.path_info == edit_frontend_path
  urlPath =  {:action => :update} 
end
%>

<%= form_with(model: @product, :url =>urlPath, local: true) do |form| %>

  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> ブログを保存できませんでした。:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table style="border-collapse: separate; border-spacing: 5px;width:580px;border:0">
	<tbody>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">分野</td>
	<td>

<%= form.select :store_id, [["コドモブックス", "1"],["デジタルブックス", "2"]] %>

	</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">書名</td>
	<td>

<%= form.text_field :syohin, size:"40" %>

	</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">対象</td>
	<td>

<%= form.text_field :taisyo, size:"40" %>

		</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">定価</td>
	<td>

<%= form.text_field :hontai, size:"50" %>

		</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">著者</td>
	<td>

<%= form.text_field :tyosya, size:"50" %>

	</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">体裁</td>
	<td>

<%= form.text_field :teisai, size:"40" %>

	</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">内　容</td>
	<td>

<%= form.text_area :naiyo, cols:"45", rows: "5" %>

		</td></tr>
	<tr style="background-color:#5f9ea0"><td style="text-align: center">画像<br /></td>
	<td>

<%= form.file_field :img_path %>

		</td></tr>
	<tr bgcolor="#2F4F4F">
	<td style="text-align: center">送　信</td>
	<td>

<%= form.submit "送信する" %>

</td></tr>
</tbody></table>

<% end %>

<span style="font-size: small">
        ■間違って投稿された方は、ご面倒でも<a href="mailto:xxxx@xxxx.co.jp">管理人</a>までメールでご連絡ください。<br />
        ■このブログの内容欄ではhtmlタグはご利用になれません。</span>

